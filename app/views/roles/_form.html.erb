<%= form_for @role do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= @role.errors.count %> Fehler verhinderten die Erstellung dieses Rechte-Profils:</h2>
      <ul>
        <% @role.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <b><%= f.label "Name:" %><br /></b>
  <%= f.text_field :name %><br />
  <br />
  <b> <%= f.label "Rechte:" %> </b>

  <table>
  <th></th>
  <th></th>
  <tr>
  <td><%= f.label "Berichte lesen" %></td>
  <td><%= f.check_box :read %></td>
  </tr>
  <tr>
  <td><%= f.label "Berichte erstellen" %></td>
  <td><%= f.check_box :commit %></td>
  </tr>
  <tr>
  <td><%= f.label "Berichte exportieren" %></td>
  <td><%= f.check_box :export %></td>
  </tr>
  <tr>
  <td><%= f.label "Berichte bewerten" %></td>
  <td><%= f.check_box :check %></td>
  </tr>
  <tr>
  <td><%= f.label "Auszubildende erstellen" %></td>
  <td><%= f.check_box :modify %></td>
  </tr>
  <tr>
  <td><%= f.label "Administrator" %></td>
  <td><%= f.check_box :admin %></td>
  </tr><br/>
  </table>
  <%= @role.new_record? ? f.submit('Rechte-Profil anlegen') : f.submit('Änderungen speichern') %>
  
<% end %>
