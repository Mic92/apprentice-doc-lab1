<table>
<tr>
<th>Name</th>
</tr>
<% @templates.each do |template| %>
<tr>
<td><%= template.name %></td>
<td><%= link_to 'Details', template %></td>
<td><%= link_to 'Bearbeiten', edit_template_path(template) %></td>
<td><%= link_to 'Löschen', template, :confirm => 'Sind Sie sicher?', :method => :delete %></td>
</tr>
<% end %>
</table>
<br />

<%= link_to 'Neue Template', new_template_path %>
