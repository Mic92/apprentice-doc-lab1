<table>
<tr>
<th>Name</th>
</tr>

<% @ihks.each do |ihk| %>
<tr>
<td><%= ihk.name %></td>
<td><%= link_to 'Details', ihk %></td>
<td><%= link_to 'Bearbeiten', edit_ihk_path(ihk) %></td>
<td><%= link_to 'Löschen', ihk, :confirm => 'Sind Sie sicher?', :method => :delete %></td>
</tr>
<% end %>
</table>
<br />

<%= link_to 'Neue IHK', new_ihk_path %>
