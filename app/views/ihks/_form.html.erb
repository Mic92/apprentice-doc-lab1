<%= form_for(@ihk) do |f| %>
  <% if @ihk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ihk.errors.count, "error") %> verhinderte, dass diese IHK gespeichert werden konnte: :</h2>

      <ul>
      <% @ihk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :maxlength => 200, :size => 20 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

